
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/setha-gram
    username: postgres
    password: setha
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
      database-platform: org.hibernate.dialect.PostgreSQLDialect